angular
// load app namespace
.module('app')
// load configure function
.config(configure);

// set dependencies
configure.$inject = ['$locationProvider'];

// configure function
function configure($locationProvider) {
	
};